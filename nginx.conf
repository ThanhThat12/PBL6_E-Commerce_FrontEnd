server {
    listen 80;
    
    # Thiết lập thư mục gốc của ứng dụng
    root /usr/share/nginx/html;
    index index.html index.htm;
    
    # 🌟 Cấu hình cốt lõi cho SPA Routing
    location / {
        try_files $uri $uri/ /index.html; 
    }
    
    # (Tùy chọn) Có thể thêm cấu hình cho API calls nếu cần:
    # location /api/ {
    #     proxy_pass http://ecommerce_backend_dev:8081/api/;
    # }
}